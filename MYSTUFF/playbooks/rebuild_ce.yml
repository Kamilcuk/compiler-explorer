---
- name: Full CE rebuild from fork
  hosts: all
  become: true
  gather_facts: false

  vars_files:
    - vars/common.yml

  tasks:
    - name: Deploy/update CE source
      ansible.builtin.include_tasks:
        file: tasks/deploy_ce.yml